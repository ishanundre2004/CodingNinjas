class Solution
{
public:
    int largest(vector<int> &arr, int n)
    {
        int largest = 0;
        for (int i=1; i<n; i++)
        {
            if (arr[i]>arr[largest]){
                arr[largest]=arr[i];
            }
        }
        return arr[largest];
    }
};
